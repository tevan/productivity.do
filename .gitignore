node_modules/
dist/
.env
*.db
*.db-wal
*.db-shm
*.db.bak*
data/
uploads/
.DS_Store
backend/db/db.sqlite

# Reference library — copyrighted material + internal-only research kept local.
# The whole directory is ignored. If we ever decide to publish anything from here,
# add a specific !path/to/file.md exception below.
docs/reference-library/

# Claude Code project-local runtime (lock files, scheduled tasks)
.claude/

# Scratch / handoff drafts that aren't part of the codebase. Anything that
# should ship lives in docs/ (internal) or marketing/ (public).
/tmp/

# Founder's private inbox — appended to by the in-app thoughts widget.
docs/internal/founder-thoughts.log
